# Простая бинаризация  # Заголовок секции с простым порогом
ret, binary = cv2.threshold(blurred,  # Применяем простую пороговую обработку
                            127,  # Пороговое значение
                            255,  # Максимальное значение для белого
                            cv2.THRESH_BINARY)  # Тип бинаризации: черно-белая

# Адаптивная бинаризация  # Заголовок секции с адаптивным порогом
adaptive = cv2.adaptiveThreshold(blurred,  # Применяем адаптивную пороговую обработку
                                 255,  # Максимальное значение для белого
                                 cv2.ADAPTIVE_THRESH_GAUSSIAN_C,  # Метод расчета порога
                                 cv2.THRESH_BINARY,  # Тип бинаризации: черно-белая
                                 11,  # Размер блока для локального порога
                                 2)  # Константа, вычитаемая из среднего
