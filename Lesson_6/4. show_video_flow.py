while True:                 # Бесконечный цикл для чтения кадров
 ret, frame = cap.read()    # Читаем очередной кадр из видео
 
 if not ret:    # Если кадр не получен
 break          # Выходим из цикла (конец видео или ошибка)
 
 cv2.imshow('Video', frame)  # Показываем текущий кадр в окне
 
 if cv2.waitKey(25) & 0xFF == ord('q'):     # Останавливаемся при нажатии клавиши q
 break                                      # Выходим из цикла по запросу пользователя
